const http = require("http"); const data = JSON.stringify({ email: "test@map.ps", phone: "0599222222", password: "1234" }); const options = { hostname: "localhost", port: 3000, path: "/api/auth/login", method: "POST", headers: { "Content-Type": "application/json", "Content-Length": Buffer.byteLength(data) } }; const req = http.request(options, res => { console.log("status", res.statusCode); let body = ""; res.on("data", chunk => body += chunk); res.on("end", () => console.log("body", body)); }); req.on("error", e => console.error("req error", e.message)); req.write(data); req.end();
