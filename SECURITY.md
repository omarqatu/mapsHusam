# مراجعة الأمن والجاهزية التشغيلية — PSM

**تاريخ المراجعة:** 2026-09-25
**النطاق:** `server.js`، ملفات البيئة في جذر المشروع، `web.config`، وكل `frontend-react`، بما في ذلك إعداد Vite والملفات العامة. لم تشمل المراجعة مجلدات الباك اند الأخرى.
**القرار الحالي:** أُنجزت تغييرات احتواء الأسرار المحلية وحجب ملفات المصدر وترقية الحزم، واجتازت الفحوص المتاحة. تدوير كلمة مرور قاعدة البيانات الخارجية والتحقق عبر IIS الفعلي واختبار الحمل ما زالت مطلوبة قبل اعتبار النشر العام الواسع جاهزاً.

هذه مراجعة ساكنة مع فحوص بناء وتبعيات. لم يُجر اختبار ضغط على قاعدة البيانات أو GeoServer الفعلي، ولم تُختبر كل مسارات التطبيق بصلاحيات حسابات مختلفة. لذلك لا تُفهم تقديرات السعة أدناه على أنها ضمان.

---

## النتائج التي يجب التعامل معها أولاً

### حرج — ملف `.env` متتبّع في Git

كان `.env` متتبّعاً في Git رغم وجوده في `.gitignore`. أزيل الآن من فهرس Git فقط، وبقي الملف المحلي موجوداً. لم تُعرض قيمه في هذا التقرير. إزالة الملف من الفهرس لا تمحو نسخه من تاريخ Git.

**ما تم:** دُوّر `JWT_SECRET` المحلي إلى قيمة عشوائية قوية، وأزيل `.env` من الفهرس مع إبقاء نسخة العمل. سيؤدي تحميل السر الجديد عند إعادة تشغيل Node إلى إبطال الجلسات الحالية، وعلى المستخدمين تسجيل الدخول مجدداً. يجب تدوير كلمة مرور PostgreSQL بالتنسيق مع كل نسخة تطبيق تستخدم قاعدة البيانات نفسها؛ هذه الخطوة معلقة لتأكيد هل قاعدة `.env` مشتركة مع الإنتاج. متغيرا `GEOSERVER_USER` و`GEOSERVER_PASSWORD` غير مستخدمين في `server.js` ضمن النطاق المفحوص؛ دوّرهما في GeoServer فقط إذا كانا صالحين ومستخدمين من خدمة أخرى.

ما زالت إزالة السر من **تاريخ Git والنسخ الاحتياطية ومخرجات CI** مطلوبة؛ حذف الملف من الفهرس لا ينظف التاريخ ولا يغير نسخ المطورين.

نُظفت ملفات الأمثلة من بيانات الاعتماد الفعلية واستُبدلت بقيم شكلية. أبقِ ملفات الأمثلة خالية من الأسرار عند أي تعديل لاحق.

### تبعيات الباك اند — تم التحديث واجتياز npm audit

قبل التحديث وجد `npm audit` **7 ثغرات إنتاجية: 1 حرجة، 3 عالية، و3 متوسطة** في `bcrypt@5.1.1`، و`http-proxy-middleware@2.0.6`، و`express`/`qs`/`tar`.

| الحزمة الحالية | النتيجة | المعالجة المقترحة |
|---|---|---|
| `bcrypt@5.1.1` عبر `@mapbox/node-pre-gyp` و`tar` | عالية/حرجة في الاعتماد العابر | رُقي إلى `bcrypt@6`، وتأكد npm من خلو شجرة الباك اند من التحذيرات. |
| `http-proxy-middleware@2.0.6` | عالية، منها احتمال حجب الخدمة | عالجها `npm audit fix` ضمن نطاق التوافق، واجتاز التدقيق الحالي. اختبر GeoServer وWFS-T وظيفياً في بيئة المشروع قبل النشر. |
| `express` و`qs` | متوسطة | عالجتها تحديثات ملف القفل؛ التدقيق الحالي لا يعرض ثغرات. |

روابط التحذيرات المباشرة: [http-proxy-middleware DoS](https://github.com/advisories/GHSA-c7qv-q95q-8v27)، [tar DoS](https://github.com/advisories/GHSA-23hp-3jrh-7fpw)، [qs](https://github.com/advisories/GHSA-x5fp-wj9c-mxmx).

> ملاحظة: ظهور `tar` في شجرة الإنتاج لا يعني أن كل ثغرات معالجة الأرشيف قابلة للوصول عبر HTTP في هذا التطبيق؛ لكنه يظل اعتماداً ضعيفاً يجب تحديثه. ثغرة البروكسي أكثر اتصالاً بمسار GeoServer المكشوف.

### Vite وتبعيات التطوير — تم التحديث واجتياز npm audit

قبل التحديث وجد فحص `frontend-react` **3 ثغرات تطويرية: 2 عالية و1 متوسطة** في Vite/esbuild وnanoid. رُقي Vite إلى `8.3.1` و`@vitejs/plugin-react` إلى `6.1.1` لتوافق Vite 8. يتطلب Vite 8 Node `20.19+` أو `22.12+`؛ بيئة الفحص كانت Node `22.19.0`. فحص npm الحالي للواجهة يعرض صفراً من الثغرات.

لا تشغّل `vite --host 0.0.0.0` أو خادم التطوير على شبكة عامة. استخدم ناتج البناء للإنتاج، وحدّث Vite إلى إصدار مدعوم، ثم اختبر صفحات المشروع والإضافات قبل اعتماد الترقية. [تحذير Vite/esbuild](https://github.com/advisories/GHSA-67mh-4wv8-2f99)، [تحذير Vite على Windows](https://github.com/advisories/GHSA-fx2h-pf6j-xcff)، [تحذير nanoid](https://github.com/advisories/GHSA-2v37-7h3g-55p8).

### حجب ملفات المصدر — أضيفت قواعد، وتبقى مراجعة IIS الفعلي

كان Node يعمل بوضع `report` الافتراضي، كما أن قاعدة IIS للملفات الثابتة كانت تسمح بأي ملف فعلي بامتدادات JS/CSS والصور والخطوط. أضيفت قاعدة IIS ترفض ملفات المصدر والأسرار ومجلدات التطوير، وضُبط `STATIC_ALLOWLIST_MODE=enforce` في `.env` المحلي وملفي المثال. حارس Node وحده لا يحمي الطلبات التي يجيب عنها IIS قبل Node.

**الاختبار التالي قبل الإطلاق:** من IIS الفعلي، جرّب `/server.js` و`/.env` و`/package-lock.json` و`/frontend-react/package.json` و`/node_modules/…` و`/dist/…`. يجب أن ترجع 404 دون محتوى. اختبار XML ومطابقة المسارات محلياً نجح، لكنه لا يثبت إعداد IIS الجاري أو وجود قواعد موروثة.

### عالٍ — مسارات API قد تستهلك الذاكرة والاتصالات مع الحمل

- `/api/service-requests` يجلب سجلات من دون ترقيم صفحات أو حد أعلى.
- `/api/search-features-batch` يقبل قائمة معرفات من دون حد عددي واضح، ويرجع صفوفاً كاملة عبر `SELECT *`.
- مسارات إدارية وتقارير أخرى تجلب مجموعات كبيرة، وبعضها يكوّن طلبات قاعدة بيانات إضافية لكل سجل مكتمل باستخدام `Promise.all`.
- `/api/search-features` يحد النتيجة بـ2000، لكن `SELECT *` مع تحويل GeoJSON لكل صف قد يستهلك CPU وذاكرة عندما تتكرر طلبات البحث.
- لا يوجد محدد معدل عام فعّال على `/api/*`: كائن `apiLimiter` موجود لكن تطبيقه معلّق. `ENABLE_RATE_LIMITING=true` لا يفعّله حالياً.
- `log-map-event` يتطلب جلسة، لكن المستخدم غير المحدود افتراضياً يستطيع توليد أحداث كثيرة وكتابة صف لكل حدث؛ الحد `request_limit` اختياري وافتراضيه غير محدود، وهو حد استخدام وليس حماية كافية من الإغراق.
- `web.config` يسمح بجسم طلب يصل إلى 50 MB، بينما محلل JSON الافتراضي في Express يقبل 100 KB تقريباً ومحلل XML مضبوط على 2 MB. وحّد الحدود حسب الحاجة، وقلل حد IIS العام حتى لا يحتفظ الوسيط بطلبات ضخمة غير لازمة.

**الإجراء:** أضف ترقيم صفحات وحدوداً قصوى لكل مجموعة، حدّاً لعدد معرفات الدفعات وحجم الاستجابة، وخنقاً مستقلاً حسب المستخدم/IP لمسارات البحث والتسجيل. راجع الفهارس على أعمدة `service_requests` و`map_service_stats` وحقول البحث المكاني. لا تفعّل حد IP عاماً قبل ضبط `trust proxy` والتحقق من عنوان العميل الحقيقي، كي لا تُجمع كل الزيارات تحت IP واحد.

---

## المصادقة وحدود المحاولات والسلوك الحالي

| الحالة | السلوك المكتشف في الكود/البيئة |
|---|---|
| تسجيل الدخول والتسجيل | `authLimiter` مشترك بين المسارين، **30 طلباً لكل 15 دقيقة لكل IP** حسب `.env` الحالي. هذا يشمل المحاولات الصحيحة أيضاً، ويمكن أن يؤثر على أشخاص خلف NAT واحد. |
| كلمة المرور الخاطئة | قفل مؤقت حسب رقم الهاتف بعد **10 محاولات فاشلة خلال نافذة 15 دقيقة** (`LOGIN_MAX_FAILS` الافتراضي 10). التخزين داخل ذاكرة عملية Node؛ يعاد ضبطه عند إعادة التشغيل ولا يتشارك بين عدة عمليات. |
| كلمات المرور الجديدة | bcrypt بعشر جولات؛ الحد الأدنى 6 أحرف. ارفع الحد الأدنى إلى 12 أو استخدم سياسة عبارة مرور، مع السماح بمدير كلمات مرور. |
| الجلسات | JWT محفوظ في `localStorage` وصالح بلا انتهاء زمني حسب التعديل الحالي. يظل التحقق من `token_version` وحالة الحساب، والخروج القسري يرفع النسخة ويمنع التوكنات القديمة. سرقة التوكن أو XSS قد تمنح وصولاً مستمراً حتى الإبطال؛ `localStorage` ليس مخزناً محمياً من JavaScript. |
| حد عام على API | غير فعّال حالياً. `apiLimiter` لا يُركّب على التطبيق رغم `ENABLE_RATE_LIMITING=true`. |
| حفظ الإحصاءات `/save-stat` | 30 طلباً في الدقيقة لكل IP في ذاكرة العملية. المسار يقبل `user_id` من العميل، لذا يمكن تزوير هوية الإحصاء/تلويثه؛ لا تعتمد على هذا المعرف لإثبات الهوية أو تطبيق حصة موثوقة. |

القفل الموجود ليس «حظراً للمتصفح» موثوقاً: يمكن للمهاجم توزيع المحاولات على أرقام IP أو إعادة تشغيل العملية، والـMap محدودة بذاكرة خادم واحد. استخدم مخزناً مشتركاً مثل Redis أو آلية قاعدة بيانات إذا تعددت النسخ، وطبّق حدود IP والحساب مع رسائل عامة لا تكشف وجود الحساب.

التوكن غير المنتهي قرار تشغيل مقصود لتلبية بقاء تسجيل الدخول. يجب حماية `JWT_SECRET` جيداً، وتدويره عند الاشتباه بالتسريب. عند غياب السر أو رفضه يولّد السيرفر مفتاحاً عشوائياً مؤقتاً ويستمر؛ كل إعادة تشغيل عندها تبطل الجلسات، كما أن تشغيل أكثر من نسخة بمفاتيح مختلفة يجعل الجلسات عشوائية. الأفضل أن يفشل بدء الإنتاج إذا كان السر غير صالح.

---

## نقاط أمن إضافية تحتاج معالجة

### CORS والتحقق من المصدر

التحقق من المصدر يسمح بأي قيمة `Origin` تحتوي النص `localhost` أو `127.0.0.1`، بدلاً من مطابقة اسم المضيف بدقة. هذا أوسع من السماح بـ`http://localhost:5173` فقط. طابق الـOrigin كاملاً من قائمة سماح صريحة، وافصل إعداد التطوير عن الإنتاج. `ALLOWED_ORIGINS` مضبوط حالياً على ثلاثة مصادر، لكن قاعدة الاستثناء المحلية تتجاوز القائمة.

### ترويسات الصفحات الثابتة

Helmet يضيف الترويسات لردود Node، لكن IIS يقدّم الملفات الثابتة مباشرة وفق `web.config` ولا تضبط الوثيقة الحالية ترويسات HSTS أو CSP أو `X-Content-Type-Options` أو `Referrer-Policy`. أضف الترويسات في IIS أو اجعل كل الطلبات تمر عبر طبقة واحدة مختبرة. CSP الحالية في Node تمنع inline scripts وقد تختلف عن سلوك IIS؛ يجب اختبارها على الصفحات الفعلية قبل فرضها.

### حدود البيانات الشخصية والسجلات

السيرفر يطبع رقم الهاتف والبريد في بعض سجلات الدخول، كما يسجل أجسام طلبات كاملة في مسارات مثل الإحصاءات وتغيير حالة الخدمة. أزل أرقام الهاتف والبريد والأجسام من السجلات العامة، وضع مدة احتفاظ وصلاحيات وصول لسجلات الإنتاج. لا تسجل Authorization أو JWT.

### تحقق البيانات والمخرجات

- `/api/auth/verify-session` يقبل `user_id` من العميل دون JWT ويجيب بحالات `not_found` و`inactive` و`force_logout`، ما يتيح تعداداً محدوداً لحالة الحسابات. اجعل النتيجة عامة واحدة أو اربطها بتوكن فعلي.
- التسجيل يقبل كلمة مرور بطول 6؛ أسماء المستخدمين تنظف بصورة بسيطة. استمر بتشفير كلمات المرور ولا تحفظ النص الصريح، لكن ارفع سياسة كلمة المرور وراجع رسائل تعداد الحساب.
- توجد استعمالات كثيرة لـ`innerHTML` في الواجهة. جرى العثور على مواضع تستخدم `textContent` أو `sanitizeHTML`، لكن لم يكتمل فحص كل مسارات البيانات المخزنة. اختبر حقول الاسم والوصف والتعليقات والروابط القادمة من قاعدة البيانات بحثاً عن Stored XSS، واستخدم `textContent` أو escape حسب سياق HTML/attribute/URL.
- واجهات البحث العامة مقصودة لعرض بيانات الخريطة، لكنها لا تتطلب مصادقة. لذلك يلزم سقف معدل وحجم لكل طلب حتى لا تتحول إلى مصدر استنزاف.

### إعدادات Proxy والبيئة

- الكود وملفا المثال يستخدمون `TRUST_PROXY_HOPS`. اضبط عدد الوسطاء الحقيقيين في الإنتاج بعد التأكد أن IIS يزيل/يعيد إنشاء `X-Forwarded-For` ولا يثق بقيمة يرسلها العميل.
- `ENABLE_CORS` موجود في الأمثلة لكنه لا يتحكم في CORS داخل `server.js`؛ `ALLOWED_ORIGINS` هو الإعداد الفعلي.
- `.env.example` فيه `API_RATE_LIMIT` لكن المحدد العام معطّل، فيوحي الإعداد بحماية غير موجودة.
- فحص `/healthz` يتأكد من قاعدة الخدمات فقط، لا من قاعدة العقارات أو GeoServer. كما أن أخطاء الاتصال عند بدء التشغيل تُسجّل ولا توقف السيرفر. أضف readiness check لكلا قاعدتي البيانات، واجعل المراقبة تفرق بين liveness وreadiness.
- معالج `uncaughtException` يسجل الخطأ ويواصل تشغيل العملية. بعد استثناء غير معالج قد تكون حالة العملية غير سليمة؛ أنهِ العملية بأمان ودع مدير الخدمة يعيد تشغيلها، مع مراقبة وإشعار.

### ملاحظات على الصلاحيات

المسارات الإدارية الأساسية تستخدم `requireAdmin`، ومسارات الخدمة الحساسة تستخدم `requireAuth`. مسار `/api/service-requests` يقارن معرفات الاستعلام بهوية JWT ويمنع طلب بيانات مستخدم آخر؛ كما تتحقق عمليات الرد والإلغاء والدردشة من عضوية الطلب. استمر باشتقاق هوية المستخدم من الجلسة لا من `user_id` مرسل من المتصفح. راجع دورياً أي مسار جديد يقرأ أو يكتب بيانات شخصية أو حالة حساب.

---

## تقدير الحمل والسعة

الإعداد المقروء: `PG_POOL_MAX` غير مضبوط، ولذلك لكل من قاعدتي البيانات حد افتراضي **25 اتصالاً**. يمكن لعملية Node واحدة فتح حتى **25 اتصالاً لقاعدة الخدمات + 25 لقاعدة العقارات**. مهلة انتظار اتصال قاعدة البيانات 8 ثوانٍ، مهلة الاستعلام 30 ثانية، و`query_timeout` 35 ثانية. هذه حدود انتظار وفشل، وليست إثباتاً أن الخادم يحتمل 100 طلب.

| عدد الحسابات المسجلة | تقدير مشروط إذا كان عدد المتصلين الفعليين قليلاً | ما لا يمكن ضمانه من دون قياس |
|---:|---|---|
| 100 | لا يظهر من العدد وحده سبب يمنع التشغيل على إعداد متوسط | أثر البحث المكاني واستعلامات قاعدة البيانات تحت الحمل الفعلي |
| 500 | قد يعمل طبيعياً إذا كان النشاط موزعاً، والفهارس والاستضافة مناسبة | معدل الذروة، وعدد الاتصالات المتزامنة، ووقت الاستجابة |
| 1000 | عدد الحسابات بحد ذاته ليس المشكلة؛ يلزم مراقبة واختبار تدريجي | لا يوجد دليل حالي يثبت قدرة الخادم أو PostgreSQL على هذا العدد |
| 100 مستخدم يرسلون طلبات في الوقت نفسه | يوجد حد 25 اتصالاً لكل Pool، لذلك الطلبات الزائدة تنتظر في الطابور | قد يحدث بطء أو تجاوز مهلة 8 ثوانٍ أو امتلاء ذاكرة/CPU، خصوصاً مع البحث الجغرافي والقوائم غير المحدودة؛ لا يمكن الجزم بانهيار أو سلامة بلا اختبار حمل |

السيرفر Node الحالي عملية واحدة، والكاش وعدادات المحاولات وخريطة مستخدمي Socket.IO في الذاكرة المحلية. تعدد العمليات/الأجهزة يحتاج تخزيناً مشتركاً، Socket.IO adapter مناسباً، وإعداد sticky sessions عند الحاجة. لا ترفع `PG_POOL_MAX` تلقائياً: أربع عمليات مثلاً قد تفتح حتى 200 اتصال لخدمات و200 للعقارات، وقد تستنزف PostgreSQL.

**الخلاصة التشغيلية:** 100 أو 500 أو 1000 حساب لا يمكن تحويلها مباشرة إلى سعة. العامل الحاسم هو المستخدمون المتزامنون، وعدد الطلبات لكل صفحة، وزمن GeoServer، والفهارس وحجم النتائج، وموارد الخادم وحد PostgreSQL. الوضع الحالي لا يثبت احتمال 100 متزامن؛ تعامل معه على أنه غير مؤكد وقد يتدهور قبل ذلك على بعض المسارات.

---

## الفحوص التي نُفذت ونتائجها

1. `node --check server.js` — **نجح** (فحص صياغة فقط، لا يشغل السيرفر ولا يتصل بقاعدة البيانات).
2. `npm.cmd run build -- --outDir ./.security-audit-dist` داخل `frontend-react` — **نجح**، ثم أزيل مجلد البناء المؤقت.
3. لا يوجد `test` script في `package.json` أو `frontend-react/package.json` — **لا توجد مجموعة اختبارات آلية للمشروع**.
4. `npm audit` في الجذر — **0 ثغرات** بعد التحديث.
5. `npm audit` الكامل في `frontend-react` — **0 ثغرات** بعد تحديث Vite والإضافة.
6. تحقق XML من `web.config` وفحص محلي لمطابقة المسارات المحجوبة — **نجح**؛ لا يعادل اختبار IIS فعلي.
7. اختبار bcrypt hash/compare بقيمة تجريبية — **نجح** بعد تحديث bcrypt.
8. لم يُنفذ اختبار ضغط أو اختبار اختراق حي، ولم تُرسل طلبات إلى قواعد البيانات الفعلية خلال هذه المراجعة.

### خطة تحقق قبل الإطلاق

1. نسّق إنشاء دور PostgreSQL بديل محدود الصلاحيات أو تدوير كلمة مرور الدور المشترك مع كل نسخ التطبيق؛ تحقق من الاتصال ثم عطّل بيانات الاعتماد القديمة. نظّف تاريخ Git والنسخ الاحتياطية وCI بالتنسيق مع المساهمين. تحقق أيضاً من أن الإنتاج يرفض مفاتيح JWT الوهمية أو المفقودة.
2. اختبر تسجيل الدخول وكلمات المرور القديمة والبروكسي وWFS-T والبناء بعد تحديث التبعيات.
3. اختبر قاعدة حجب الملفات على IIS الفعلي وتأكد من عدم إمكانية تنزيل ملفات المصدر أو الإعدادات.
4. اختبر صلاحيات API بحساب user وprovider وadmin: قراءة طلبات مستخدم آخر، الرسائل، أرقام التواصل، تعديل الخدمة، الخروج القسري، والـadmin endpoints.
5. أضف حدود نتائج وصفحات وخنق طلبات، ثم اختبر مستخدماً يرسل نقرات/بحثاً سريعاً ودفعة معرفات كبيرة.
6. شغّل اختبار حمل في بيئة staging ببيانات مماثلة للإنتاج: 10 ثم 25 ثم 50 ثم 100 مستخدم متزامن. سجّل p95/p99، أخطاء 4xx/5xx، CPU/RAM، طوابير PostgreSQL، الاتصالات، وزمن GeoServer. أوقف التصعيد عند نمو الطابور أو اقتراب الذاكرة من حدها.
7. أضف اختبارات آلية للمصادقة والصلاحيات والتصفية والاستعلامات الحرجة، وفحوص npm audit دورية في CI.

---

## حالة الجاهزية

**لا يمكن اعتمادها الآن كنشر عام خالٍ من المخاطر.** عولجت نسخة العمل: أزيل `.env` من فهرس Git وبقي محلياً، جُدد `JWT_SECRET` المحلي، أضيف حجب ملفات في IIS، وحُدثت التبعيات إلى أن أصبح `npm audit` صفراً في الجذر والواجهة. ما زال سر قاعدة البيانات المشترك بحاجة إلى تدوير منسق، كما أن سر `.env` القديم يبقى في تاريخ Git حتى تنظيفه. يجب اختبار IIS الفعلي، الصلاحيات، ووظائف البروكسي، ثم إجراء اختبار حمل على staging قبل تحديد سعة المستخدمين المتزامنين. لا توجد حالياً نتيجة تثبت قدرة 100 مستخدم متزامن.

**سجل هذه المراجعة:** استبدلت التقييم السابق لأنه ذكر إعدادات لم تعد صحيحة (مدة JWT، حد تسجيل الدخول، وحالة CSP/Rate Limiting) ولم يوثق نتيجة npm audit أو تتبع `.env`.
